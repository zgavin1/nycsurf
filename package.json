{
  "name": "nyc-surf",
  "version": "1.0.0",
  "description": "Forecasting app for NYC beach conditions",
  "engines": {
    "node": "8.9.x"
  },
  "scripts": {
    "start": "node server",
    "heroku-postbuild": "cd react/ && npm install && npm install --only=dev --no-shrinkwrap && npm run build-css && npm run build"
  },
  "cacheDirectories": [
    "node_modules",
    "react/node_modules"
  ],
  "dependencies": {
    "express": "^4.16.2",
    "request": "^2.83.0"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/zgavin1/nyc-surf.git"
  },
  "keywords": [
    "node",
    "heroku",
    "create-react-app",
    "react"
  ],
  "license": "MIT",
  "devDependencies": {}
}
